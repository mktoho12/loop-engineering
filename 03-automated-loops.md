# 自動ループの実装

2026-08-01。手動でループの骨格を掴んだので、シェルスクリプト + `claude -p` で自動化した。

## 設計方針

ユーザーの提案:

> トークンを節約するために、発見ループは対象が前回と何も変わってなかったらすぐ終了して
> つぎのループまで待機するようにして、修正ループは issue がなかったらすぐ終了して次のループまで待機

これは**早期終了ガード**の設計。ループエンジニアリングで一番効く最適化のひとつ。

**要点: ガードを純粋なシェルで書けば、仕事がない時のトークン消費が完全にゼロになる。**
`claude` を起動しなければ、コンテキストの読み込みすら発生しない。

## ファイル構成

```
loops/
├── config.sh            # 共通設定・run_claude / early_exit
├── discover-loop.sh     # 発見ループ
├── fix-loop.sh          # 修正ループ
├── format-stream.jq     # stream-json を人間可読に整形
├── prompts/
│   ├── discover.md      # 発見ループのプロンプト
│   └── fix.md           # 修正ループのプロンプト
├── state/               # 前回実行の状態（HEAD、観点のインデックス等）
└── logs/                # 整形ログ (.log) と生 JSON (.jsonl)
```

## 早期終了ガードの設計

### 修正ループ: 素直

```bash
CANDIDATES=$(gh issue list --state open ... )
[ "$(echo "$CANDIDATES" | jq 'length')" -eq 0 ] && early_exit "処理可能なオープン Issue がない"
```

さらに **着手済みの除外** が要る。同じ Issue を二重に処理しないよう、
`fix/issue-N` というブランチの存在（ローカル + リモート）と worktree の存在で判定する。

### 発見ループ: ここが難しい

素朴に「HEAD が変わっていなければスキップ」にすると**既存コードのバグを永久に見逃す**。
コードが変わらなくても、前回のスキャンが不完全だった可能性は常にある。

→ **観点のローテーション**で解決した。

```bash
LENSES=(
  "security:認証・認可の欠落、入力検証の漏れ、秘密情報の混入…"
  "test-quality:テストが実質何も検証していないもの、抜けている異常系…"
  "spec-drift:README や CLAUDE.md に書かれているのに実装されていないもの…"
  "correctness:境界値・null・並行アクセスで壊れる箇所…"
)
```

ガードの条件は「**HEAD が同じ かつ 全観点を一巡済み**」。
一巡していなければ、HEAD が同じでも別の角度から見る価値がある。
コードが変わったら周回カウントをリセットする（新しいコードは全観点で見直す）。

これは手動実験の観察に基づく。**私が3回別々の観点で見るたびに、新しい問題が出てきた。**

### もう1つのガード: バックログ上限

```bash
[ "$OPEN_COUNT" -ge 15 ] && early_exit "オープン Issue が上限に達している。修正が追いつくまで発見を止める"
```

直す先が詰まっているのに発見だけ進めても意味がない。

### ガードの動作確認

設計しただけでは意味がないので、実際に発火することを確認した:

| ガード | 動作 | claude 起動 |
|---|---|---|
| 発見: HEAD 同じ + 一巡済み | ✅ SKIP | なし |
| 修正: 候補 Issue ゼロ | ✅ SKIP | なし |
| 修正: 着手済みブランチの除外 | ✅ スキップして次の Issue へ | — |

## 隔離: git worktree

ユーザーの指摘:

> 作業フォルダが1つしかないから複数のissueを同時に進められなかったりする？

その通りだった。当初の設計は `git switch` でブランチを切り替える前提で、
同時に2つ走らせると互いの作業を壊す。

Osmani の6部品にも「**隔離された作業場所** — 複数エージェントの成果物を保護」が入っている。

修正ループを worktree ベースに変更した:

```bash
git worktree add -b "fix/issue-$N" "$WORKTREE_ROOT/issue-$N" origin/main
```

**実装上の注意点**（ハマったところ）:

1. **`node_modules` がない** — worktree には git 管理下のファイルしか来ない。
   毎回 `bun install` すると遅いので、メインからシンボリックリンクを張る。
   プロンプトに「`bun install` を実行しないでください（リンク先を壊します）」と明記
2. **`.dev.vars` がない** — gitignore されているのでコピーが必要。ないとテストが落ちる
3. **後片付け** — `trap cleanup EXIT` で、成功しても失敗しても worktree を削除する。
   ブランチは残す（PR の参照先なので）

発見ループは読むだけなので worktree は不要。メインの作業ディレクトリを使う。

## ハマったバグ3つ

自動化で最初にぶつかったのは、ループの設計ではなく**シェルスクリプトの初歩的な問題**だった。

### 1. bash 3.2 の空配列展開

```
./config.sh: line 57: timeout_cmd[@]: unbound variable
```

macOS 標準の bash は 3.2 で、`set -u` の下では空配列 `"${arr[@]}"` の展開に失敗する。
配列をやめて文字列prefixにして解決。

### 2. stdin を明示していない（これが一番厄介だった）

ログが 0 バイトのまま、claude が何も出力せずに終わる。原因はこれ:

```
Warning: no stdin data received in 3s, proceeding without it.
```

`claude -p` は標準入力を読もうとする。スクリプトから起動すると入力待ちで固まる。
**`< /dev/null` が必須。**

### 3. text 形式は「全部終わってから一括出力」

`--output-format` を指定しないと、処理中はログが空のまま。
数分〜十数分かかる処理では「動いていない」と誤認する。

ユーザーからも「2分待ったがログが出ない」と指摘があった。実際には動いていた。

→ **`--output-format stream-json --verbose` + jq で整形**して解決。

```
── セッション開始 (claude-opus-5[1m]) cwd=/Users/mktoho/work/ai/world-issue-tracker
💬 まず現状を確認します
🔧 Bash: git status && git log --oneline -3
📖 Read: .../apps/api/src/routes/issues.ts
🤖 Task(general-purpose): レビュアーとして検証
── 終了 (success) turns=28 時間=370s コスト=$2.017
```

生 JSON も `.jsonl` に残す（整形で情報が落ちるため）。

**教訓**: 無人ループで一番困るのは「動いているのか死んでいるのか分からない」状態。
進捗の可視化は快適さの問題ではなく、**運用可能性の問題**。

## 実行結果

### Issue #2（認可の欠落）→ PR #6

370秒、$2.017、28ターン。CI 緑。**マージ済み**（`Closes #2` で Issue も自動クローズ）。

ログから確認できた良い挙動:

- **Red → Green を実際に経由した**。先に落ちるテストを書き、脆弱性が再現することを確認してから直した
- **レビュアーが敵対的検証をした** — `checkOwnership` をわざとスタブ化し、
  「テストが本当に効いているか」を確かめた
- レビュアーの指摘3件を受けて修正し、再検証（37 tests、lint/check/build 全部緑）

実装の質も高かった:

- `!row.user_id` のチェック — `user_id` が NULL の既存レコード（認証追加前のデータ）を
  誰も操作できないよう倒している
- **認可をバリデーションより先に** — 他人の Issue にバリデーションエラーの詳細を漏らさない
- **TOCTOU 対策** — チェックと書き込みの間で行が変わる可能性に備え、SQL 自体にも所有者条件

### Issue #3（テストの重複）→ PR #7

389秒、$2.15、31ターン。**worktree での初回実行。**

ここで**ミューテーションテストの応酬**が起きた。今日一番の見どころ:

1. 作り手が「重複テストを削除しても検出力は落ちない」と主張。
   `requireAuth` を外す変異体を作り、削除後のテストでも検出できることを示した
2. **レビュアーが反証** — 「401 を返しつつ削除は実行する」という別の変異体を構築。
   これは削除しようとしているテストだけが捕まえていた
3. 作り手が再現して認め、**削除ではなく401テストに統合**する形に修正。
   両方の変異体を捕まえることを確認してから PR を出した

作り手の自己検証を、レビュアーがより強い変異体で破った。**分離の価値がはっきり出た事例。**

この知見はプロンプトに反映した:

> テストの検出力を疑うときは、変異体を作って確かめるよう指示してください。
> このとき、**ステータスコードを変える変異体だけでなく、副作用を変える変異体も試す**
> よう指示してください。前者しか試さないと、テストの検出力を過大評価します。

## コストの実測

| 実行 | 時間 | コスト | ターン |
|---|---|---|---|
| Issue #2 の修正 | 370s | $2.017 | 28 |
| Issue #3 の修正 | 389s | $2.15 | 31 |
| **早期終了（ガード発動）** | **< 1s** | **$0** | **0** |

ガードが効いたときのコストがゼロなのが効いている。
1時間ごとに回しても、仕事がなければ課金されない。

なお Business Insider の記事にあった「エージェント1体で通常の約4倍、
複数構成で約15倍のトークン消費」は、この規模でも実感できる。
1 Issue あたり $2 は、レビュアー（サブエージェント）を含めての値段。

## 言語設定

ログが英語で出ていたので、プロンプトに明記して日本語化した。

```markdown
## 言語

**思考の記述・進捗の説明・報告はすべて日本語で書いてください。** このログは日本語話者が読みます。
```

**サブエージェント（レビュアー）への指示にも明記が必要**。
親のプロンプトに書いただけでは、Task で起動される子には伝わらない。

## 同時実行の実地検証

発見ループと修正ループを実際に並行で走らせた。

### 結果: ループが連結した

これが今回の一番の成果。

1. **発見ループ**が security 観点でスキャンし、Issue を3件作成（448秒、$2.288）
2. **修正ループ**が、その中の **Issue #8 を即座に拾って作業を開始**

発見 → キュー → 修正 の流れが、人間の介在なしに自動で繋がった。
設計した構造が実際に機能することが確認できた。

worktree も正しく分離されていた:

```
$ git worktree list
/Users/mktoho/work/ai/world-issue-tracker  edf699c [main]           ← 発見ループが使用（読むだけ）
/Users/mktoho/work/ai/.worktrees/issue-8   edf699c [fix/issue-8]    ← 修正ループが使用（書き込み）
```

メインの作業ツリーは一切汚れなかった。

### 発見ループが見つけたもの

3件とも実在するセキュリティ問題だった:

| # | 内容 |
|---|---|
| #8 | 公開 GET が `SELECT *` で Clerk User ID を未認証の第三者に露出 |
| #9 | Cookie セッション認証を受け付けているが Origin 検証も CSRF 対策も無い |
| #10 | `clerkMiddleware` を no-op にモックしており認証・認可の回帰を検出できない |

**Issue の質が高かった。** #8 では:

- **本番 API に実際に curl を投げて確認**（`user_id` キーがレスポンスに含まれることを実証）
- ローカルでも再現テストを実施（確認後に削除、作業ツリーはクリーン）
- 症状だけでなく**構造的リスク**を指摘:
  > 今後カラムを追加した場合、`SELECT *` のままだと追加した瞬間に自動的に公開される。
  > これが構造的に一番まずい点。

### 「Issue にしなかったもの」の判断も良かった

ログを見ると、以下は**却下**していた:

- 秘密情報の混入 → 確認したが問題なし
- `/health` の情報漏洩 → 疑って本番に curl を投げたが、正常時は漏れない。
  エラー時の内容は DB を壊さないと確認できないため、
  **「推測になるため Issue にはしない」と判断**

プロンプトの「推測で Issue を立てないでください」が効いていた。
ノイズを出さないことも発見ループの品質のうち。

### 見つかったバグ: bash 3.2 のマルチバイト変数展開

同時実行の初回で発見ループが即死した:

```
./discover-loop.sh: line 102: LENS_NAME」: unbound variable
```

原因は日本語の直後に変数を書いたこと:

```bash
SCAN_HINT="…今回は「$LENS_NAME」の観点で…"
```

bash 3.2 は全角の `」` を変数名の一部と解釈し、`LENS_NAME」` という未定義変数を参照する。
`set -u` で落ちる。

修正は波括弧で境界を明示するだけ:

```bash
SCAN_HINT="…今回は「${LENS_NAME}」の観点で…"
```

**この分岐は「HEAD が前回と同じ」の経路にしかないので、初回実行では必ず踏むバグだった。**
dry-run では通っていた（プロンプト組み立ての手前で終わるため）。

### プロンプトの不備: 禁止の代替を書いていなかった

ユーザーからの指摘:

> `bun install` は禁止ですって書いてたけどどういう意味？ なぜ禁止なのか理由がわからないのと、
> もし妥当な理由があって禁止されてたら人間に依頼して欲しいのですよ

これは設計の不備だった。プロンプトには「`bun install` を実行しないでください
（リンク先を壊します）」とだけ書いてあり、**新しい依存が必要になった場合の導線がなかった。**

エージェントは `bun install` できず、人間に頼む手段もないので、手詰まりになるか、
最悪リンクを壊してでも進めてしまう可能性がある。

修正: 「詰まったとき」の節に依存追加のケースを追加し、禁止の理由も詳しく書いた。

> **`bun install` / `bun add` は実行せず、Issue にコメントして人間に依頼してください。**
> コメントに書くこと: どのパッケージか、なぜ必要か、どのワークスペースに入れるか

**教訓**: 無人ループでは「禁止」だけでは足りない。
**禁止したら、代わりに何をすべきかを必ず書く。** 人間なら聞きに来るが、
ループは聞きに来る先がないと詰むか暴走する。

### Issue #8 の修正結果（PR #11）

879秒、$5.322、**76ターン**。これまでで最大規模。CI 緑、48 tests（36 → 48）。

実装が優れていた。**二段構え**にしている:

```typescript
// 公開してよいカラムを一箇所で定義
export const PUBLIC_ISSUE_COLUMNS = ["id", "title", ...] as const;

// ① SELECT 句で絞る（DB から引かない）
export const PUBLIC_SELECT = PUBLIC_ISSUE_COLUMNS.join(", ");

// ② 返す直前にも落とす（SELECT の書き漏れがあってもここで落ちる）
export function toPublicIssue(row) { ... }
```

Issue が指摘した構造的リスク（カラム追加時に自動公開される）に対して、
`PUBLIC_ISSUE_COLUMNS` を**単一の判断ポイント**にしたのが的確。

ここでも**ミューテーションの応酬**が起きた:

- レビュアーの指摘: 「公開フィールドのテストが `/issues` をクエリパラメータなしでしか
  叩いていないため、**フィルタ/ページネーション経路でだけ漏れる回帰は検出できない**」
- 作り手が変異体 M11（`?scope=` 指定時だけ漏らす）を作って再現。45テスト全部通ることを確認
- テストを追加し、変異体が捕まることを確認してから PR

最終的に、元の脆弱な実装に戻す変異体が **8テストを落とす**ようになった（当初は5）。

### 課題: 長い作業では言語指定が薄れる

76ターンの作業中、**発話が英語に戻っていた**。ログを見ると2番目だけ日本語で、その後は英語。

特に**デバッグで難航した局面**で英語に流れる傾向があった
（`PUBLIC_ISSUE_COLUMNS` が undefined になる問題の調査で長時間かかった箇所）。

対策としてプロンプトを強化した:

1. **冒頭の指定を強める** — 「これは最初の数ターンだけの指示ではありません。作業が長引いても、
   デバッグで難航しても、最後まで日本語を使ってください」と明示
2. **末尾にも再掲を置く** — 長いプロンプトでは最後の指示が効きやすい

**教訓**: 長時間走るループでは、**冒頭の指示は薄れる**。
重要な制約は繰り返すか、末尾に置く。

## 発見ループ2周目: 重複排除の検証

観点が `security` → `test-quality` にローテーションした状態で2周目を実行（332秒、$1.916）。
確認したかったこと3つが、すべて確認できた。

### 1. 重複排除が機能した

ログの途中でこう判断していた:

> まず既存 Issue #10 との重複を避けたいので、その内容を確認します。
> → #10 は「モックが実物と乖離している」を既にカバーしています。
> 　 私が見つけた3件はいずれも別の根です。

結果、`test-quality` 領域に4件の Issue が並んだが、**すべて別の根**だった:

| # | 根 | キー |
|---|---|---|
| #10 | 認証モックが実物と乖離 | `[api/test:auth-mock]` |
| #12 | アサーションが弱い（`toBeDefined()` のみ） | `[api/test:updated-at]` |
| #13 | 異常系テストの欠如 | `[api/test:query-validation]` |
| #14 | テストスキーマが実マイグレーションと乖離 | `[api/test:schema-drift]` |

**機械可読なキーを付ける設計が効いた。** タイトルを見るだけで領域と根が分かるので、
重複判定に LLM の推論を使わずに済む（既存 Issue のタイトル一覧を渡すだけでよい）。

### 2. 強化した言語指定が効いた

**全発話が日本語のまま最後まで維持された。** 冒頭の強化 + 末尾の再掲が機能した。

### 3. 見つけた3件がどれも本物（全部ミューテーションで実証）

**#12 が特に秀逸**。`updated_at = datetime('now')` を実装から丸ごと削除しても
**46テスト全部通る**ことを実証した。

なぜ空振りしていたかの説明も的確:

> `updated_at` は `NOT NULL DEFAULT (datetime('now'))` なので、**INSERT の時点で常に値が
> 入っている**。したがって `toBeDefined()` は PATCH の挙動と無関係に必ず成功する。

**#14 の検証方法も見事**。実マイグレーションに一時的にカラムを追加して
（`0003_tmp_probe.sql` を作成）、テストが古い手書きスキーマを使い続けるため
緑のまま通ることを実証し、その後ファイルを削除している。

しかも捻れを指摘していた:

> `does not expose future internal columns` テストは「カラム追加で漏れないこと」を守る
> 意図なのに、そのカラムが**実マイグレーション由来だと検証対象にすら入らない**

### 後片付けも完璧だった

一時ファイル（`tmp-probe.test.ts`、`tmp-probe2.test.ts`、`0003_tmp_probe.sql`）を
すべて削除し、`git status` がクリーンであることを確認してから終了していた。
`/tmp` に置いたバックアップも消していた。

## 残っている課題

- **失敗時のリトライ** — claude が非ゼロ終了したときの扱いが「WARN を出すだけ」
- **バックログ上限の実地検証** — Issue が15件を超えたときに発見ループが止まるか
