# 止まったループを動かし直す（2026-08-18）

7 日間、ループは「正常に」動き続けていた。exit code は 0、launchd から見ても成功、
監視ループも無風。だが **1 件も修正していなかった**。

この日はその発見から始まり、原因の除去、再発防止、そして詰まりの本丸だった
`#98`（本番が Clerk の開発用インスタンスで動いている）の解消までを行った。

---

## 何が起きていたか

発端は「issue 一覧で止まってるものはある？ そもそもループは動いてるの？」という問い。

`launchctl` を見ると fix ループは 703 回（調査時点）起動し、すべて exit 0 で終了していた。
直近も 30 分前に動いている。だがログを遡ると、**最後に実際の修正をしたのは 7 日前**
（`worktree を作成した` の最終記録が 2026-08-11 12:00）だった。

```
[2026-08-18 01:30:05] === fix ループを開始 ===
[2026-08-18 01:30:07] Issue #98 はブランチが既にあるのでスキップ
[2026-08-18 01:30:07] Issue #88 はブランチが既にあるのでスキップ
[2026-08-18 01:30:07] Issue #95 はブランチが既にあるのでスキップ
[2026-08-18 01:30:07] SKIP: オープン Issue は 3 件あるが、すべて着手済み
[2026-08-18 01:30:07] === fix ループを終了 ===
```

`needs-discussion` の付いた Issue は対象外なので、候補は 3 件しかなかった。
そしてその 3 件すべてに、**過去の失敗が残したブランチが居座っていた**。

| Issue | ブランチの状態 | 回収されなかった理由 |
|---|---|---|
| #98 | 未マージのコミット 1 件（474 行の実装） | 空ではないので `fix-loop.sh` の回収対象外 |
| #95 | 未マージのコミット 2 件 | 同上。8/10 に $22.86 を使って異常終了した残骸 |
| #88 | 実体は空。ただし stale な remote-tracking ref が残存 | リモートにあると見なされスキップ |

`fix-loop.sh` には「空ブランチなら回収する」ロジックがある。API エラーで claude が
途中で死んだときの残骸を拾うためのもので、実際そのために書かれていた。
だが **中身のあるブランチは回収できない**。異常終了しても作業が途中まで進んでいれば、
そのブランチは永久に候補を塞ぐ。

つまり **「失敗が半端に成功していると、ループは自力で抜け出せない」**。

---

## なぜ 7 日も気づけなかったか

これが本題である。

監視ループ（`watch-loop.sh`）は 6 種類の検査を持っていた。異常終了、連続失敗、
ハング、危険なコマンド、高コストな失敗 — どれも実際に起きた事故から作られたものだ。

しかし **検査 1〜5 はすべて `fix-*.jsonl` を読む**。そして早期終了（`early_exit`）した
実行は claude を起動しないので、**jsonl を 1 バイトも残さない**。

```
launchd から見る    → exit 0。成功
監視ループから見る  → 見るべきログが無い。無風
人間から見る        → 何も報告されない。順調に見える
```

観測系がすべて「実行の記録」を前提にしていたため、**実行しなかったことは観測できなかった**。
311 回空振りしても、誰にも届かなかった。

### 直したこと

検査 6 を足した。launchd のログから「最後に着手してからの連続スキップ回数」を数え、
しきい値（6 回 ≒ 3 時間）を超えたら報告する。

実作業の目印は `worktree を作成した` にした。ここが引き返せない地点で、以降は必ず
claude を起動する（＝ jsonl が残り、既存の検査の対象になる）。
`=== fix ループを開始 ===` は早期終了でも出るので目印に使えない。

**ただし最初の実装は誤検知した。** その日のうちに `needs-discussion` だけが残って
候補がゼロになり、「仕事が無いだけの正常な待機」まで異常として鳴り続けた。

早期終了には 2 種類あり、意味が正反対だった。

```
「すべて着手済み」                  … 仕事はあるのに手が出せていない → 異常
「処理可能なオープン Issue がない」  … そもそも仕事が無い            → 正常な待機
```

後者は過去に 90 回以上出ている普通の状態で、混ぜると人間が議論待ちを片付けるまで
永久に鳴る。前者だけを数え、後者が現れたら連続を打ち切るようにした。

### 監視ループ自身が動いていなかった

さらに悪いことに、修正の過程で `${...}` を省いた箇所を作ってしまった。

```bash
say "... （しきい値 $SKIP_STREAK_THRESHOLD）"
```

直後が全角の `）` だったため bash が変数名の一部と解釈し、`set -u` で
`unbound variable` になって **監視ループそのものが落ちていた**。報告以前に実行できていない。

全スクリプトを走査したところ、既存の 2 箇所は無害だった（片方はコメント、
もう片方は展開が正常）。皮肉なことに、その無害なコメントは
「`$LENS_NAME」` は `LENS_NAME」` という変数名になる」という**この罠への警告**だった。
警告を書いた本人が、別の場所で同じ罠を踏んだ。

---

## 詰まりを解いた手順

### 1. 残骸ブランチの除去

- `#88`: `origin/main` に完全に含まれることを確認（差分ゼロ）してから削除。
  リモート側は PR #103 のマージ時に既に消えており、ローカルの remote-tracking ref が
  古いまま残っていただけだった（`git remote prune` で解消）
- `#98` / `#95`: **空ではなかった**。中身のある作業が push されず埋まっていたので、
  そのまま push して PR にした

ここで学んだのは、ループのログ（「ブランチが既にあるのでスキップ」）を鵜呑みにすると
**「空ブランチだから消してよい」と誤読しかねない**ということ。実際には 474 行の実装が
入っていた。消す前に中身を見る手順が要る。

### 2. #95 のコンフリクト解消

`#102`（デザイン見本）がマージされた結果、`#95` とトークン定義層が二重実装になっていた。
15 ブロックのコンフリクトのうち、実質的な判断は 3 点だけだった。

- **名前が違うだけの二重定義**（`--shadow-raised` vs `--shadow-card` など 5 組）
  → 見本を持つ `#102` 側を正本とする方針が既に決まっていたので、そちらへ寄せた
- **値が食い違う 5 個** → doc に記載のある側を採用
- **`--sun`（黄 vs 茶）** → 両側とも背景と図版にしか使っておらず、文字色には未使用。
  コントラストに影響しないので doc どおり黄系に

コンフリクトは「量」ではなく「どれが本当に判断を要するか」で切り分けると小さくなる。

---

## #98: キーを差し替えれば直る、ではなかった

本丸は `#98` だった。本番サイトが Clerk の開発用インスタンスで動いており、
利用者が増えると止まる。

当初は「本番キーに差し替えるだけ」と考えていた。**これは間違いだった。**

公式ドキュメントを実際に読んで分かったこと:

- 本番インスタンスは **独自ドメインが必須**。プレビュー環境の項に
  「ホスティング事業者の提供ドメイン（`*.vercel.app` 等）では本番キーを使えない」と明記があり、
  `*.workers.dev` も同じ立場になる
- 技術的にも必然だった。本番は Clerk の Frontend API を自ドメインのサブドメインに
  CNAME で載せ、cookie を same-site として送る。開発用はそれができないため
  トークンをクエリ文字列で渡しており、ドキュメント自身が
  「ログや履歴に残り傍受され得るので本番には不十分」としている
- 開発用インスタンスの上限は **100 ユーザー**、**インスタンス間でユーザーは移行できない**

つまり `#98` は「設定ミス」ではなく **「ドメインを持っていないこと」が原因**だった。
関門（CI での検証）を入れた時点では、この前提を確認できていなかった。

### 費用は $0 だった

- Clerk は **Hobby プランのまま本番インスタンスを使える**（Custom domain は無料枠に含まれる）
- **satellite domain は不要**。あれは「複数の異なるドメイン」を 1 インスタンスで共有する機能で、
  単にアプリがサブドメインにあるだけなら該当しない（公式が冒頭で明示的に区別している）
- Google OAuth も `email` / `profile` / `openid` のみなので **審査不要**
- ドメインは既に所有していた

「有料そうに見えるが調べると無料」というケースだった。**推測で諦めず、一次情報を読むこと。**

### CLI でどこまでやれたか

| 作業 | 手段 | 結果 |
|---|---|---|
| Clerk 本番インスタンス作成 | `clerk deploy --mode human` | 対話が必要（`!` 経由は `/dev/tty` が無く不可） |
| DNS レコード 5 件 | **`flarectl`** | 一括登録。**「最大 48 時間」の警告に対し実測 5 分** |
| Workers Custom Domain | `wrangler`（`wrangler.jsonc` の `routes`） | 可 |
| Google OAuth クライアント | **不可** | 後述 |
| GCP プロジェクト作成 | `gcloud projects create` | 可 |

`wrangler` は DNS を触れない（OAuth トークンの権限が Workers 中心で、
DNS レコードの読み取りすら `Authentication error`）。`flarectl` は Cloudflare 公式で
`create-or-update` があるため冪等に実行できる。ただし **Workers 機能は持っていない**ので、
Custom Domain は `wrangler` 側の担当になる。用途で使い分ける。

**Google の OAuth クライアントだけは CLI で作れない。** 公式ヘルプに
「この API はプロジェクト内のすべての OAuth クライアントを管理する汎用 API としては使えない」
と明記されており、リダイレクト URI も設定できない。しかも
**IAP OAuth Admin API は 2026-03-19 に恒久停止済み**だった。
Terraform にも該当リソースが無く、IaC 界隈でも「Console 手動が必須」が共通認識。

---

## 途中で踏んだ罠

### `options` を渡すと env からキーを読まなくなる

`authorizedParties`（サブドメインの cookie 漏洩対策）を足したら、**188 件のテストが落ちた**。

`@hono/clerk-auth` の実装はこうなっている。

```js
const { secretKey, publishableKey, ... } = options || {
  secretKey: clerkEnv.CLERK_SECRET_KEY || "",
  publishableKey: clerkEnv.CLERK_PUBLISHABLE_KEY || "",
}
```

**`options` を渡した瞬間、env から読む経路が丸ごと消える。** Workers ではキーが
env（Secrets）にしか無いので、これは本番の認証が全滅する変更だった。

テストが捕まえたから助かった。逆に言えば、**テストが無ければ本番で初めて分かる**類の罠だった。

### 設定は「消えても正常系が通る」

`authorizedParties` も、旧オリジンからの転送も、**消えたところで正常系のテストは全部通る**。
だから「渡していること」「転送していること」自体を固定するテストを足した。

そして **意図的に該当行を消して、そのテストだけが落ちることを確認した**（変異テスト）。
これをやらないと「常に通るだけのテスト」を書いてしまう。実際、最初に書いた
`authorizedParties` のテストは、この確認をするまで本当に検証できているか不明だった。

### モジュールのモックはファイル単位で閉じない

転送のテストで Clerk をモックしたら、**無関係な web のテストが 1 件落ちた**。
`bun:test` の `mock.module` はファイルを越えて影響する。

判定ロジックを純粋な関数として `src/lib/legacy-origin.ts` に切り出して解決した。
「テストのためにモックが要る」と感じたら、**切り出せないか先に考える**方がよい。

### Custom Domain は `workers.dev` を無効にする

切り替え直後、旧 URL が 404 になった。Cloudflare の既定挙動で、
Custom Domain を設定すると `workers.dev` のサブドメインが無効になる。

PR には「旧 URL のブックマークや貼られたリンクが残る」と書いて許可リストに残したのに、
**その旧 URL 自体が消えた**ので意味がなかった。`workers_dev: true` を明示して
リクエストが Worker に届くようにし、308 で転送する形にした。

308（Permanent Redirect）にしたのは、301 と違ってメソッドと本文を変えずに引き継ぐため。
誤って POST が GET に化けると投稿が静かに消える。

---

## 結果

| 項目 | 状態 |
|---|---|
| `issues.emaker.dev` | HTTP 200 |
| Clerk のホスト | `clerk.issues.emaker.dev`（自前ドメイン） |
| publishable key | `pk_live_`（本番用） |
| Google ログイン | 動作確認済み |
| 旧 URL | 308 → 新ドメイン（パス・クエリ維持） |
| Issue #98 | CLOSED |

マージした PR: #104（API 側の関門）、#105（#95 のデザイン適用）、
#106（ドメイン定数化と `authorizedParties`）、#107（旧オリジンの転送）

---

## 残っていること

- オープン Issue 5 件はすべて `needs-discussion`。ループは触れないので、
  現在は **「仕事が無い」正常な待機状態**
- discover ループは 8/2 から停止したまま。再開すれば新しい Issue は増える
- `#79`（loop-health）に過去の異常記録が残っている

---

## この日の教訓

**1. 観測系が「実行の記録」だけを見ていると、実行しなかったことは観測できない。**
異常終了は検出できても、何もしなかったことは検出できない。後者の方が長く続く。

**2. 「正常終了」は「仕事をした」を意味しない。**
exit 0 が 703 回並んでいても、成果はゼロでありうる。終了コードは意図を語らない。

**3. 異常と正常な待機を区別しないと、警報は無視されるようになる。**
「仕事が無い」を異常として鳴らし続ければ、本物の異常が埋もれる。
検出を足すときは、**鳴らないケースの定義も同時に決める**。

**4. 前提の確認を後回しにすると、直せない関門を作る。**
`#98` の関門は「キーを差し替えれば通る」前提で作ったが、実際は独自ドメインが必要で、
その前提が崩れている間 **web のデプロイまで巻き添えで止まった**。

**5. 一次情報を読む。**
「有料そう」「CLI では無理そう」はどちらも外れた（前者は無料、後者は一部可能）。
逆に「キーを差し替えるだけ」という楽観も外れた。推測はどちらの方向にも外れる。

**6. 消えても気づけない設定には、消したら落ちるテストを書く。**
そして **本当に落ちるかを、壊して確かめる。**
