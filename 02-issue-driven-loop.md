# 設計転換: GitHub Issue をキューにする

2026-08-01。ラウンド1フェーズAの途中で、ユーザーから出た提案:

> バグを発見するループと修正するループをわけることもできるの？
> せっかく GitHub を使ってるんだから、バグを見つけたら GitHub の issue に記録して、
> ループの主目的は GitHub の issue をこなすことにしたら、いい感じに近づくんじゃないかな

これはループの構造そのものを変える提案だったので、独立した記録にする。

## Before: 1タスク = 1ループ

```
[仕様] → 作り手 → 機械的検証 → 検証役 → [完了 or 差し戻し]
```

問題点:
- 検証役が見つけた問題は**その場で処理するか捨てるかしかない**
- ラウンド1では検証役が3件見つけたが、うち1件（認可欠陥）は
  今回のスコープ外。**宙に浮いた**
- セッションが終わると発見が失われる

## After: 発見と修正を疎結合にする

```
[発見ループ]  コードを漁る → 問題を見つける → Issue を立てる
                                                    ↓
                                          ┌─────────────────┐
                                          │  GitHub Issues  │ ← キュー / 外部メモリ
                                          └─────────────────┘
                                                    ↓
                                          [人間がトリアージ]  ← Developer Feedback Loop
                                                    ↓
[修正ループ]  Issue を1つ取る → 直す → PR → 検証 → close
```

## なぜこれが良い設計なのか

### 1. 外部メモリになる

Addy Osmani が挙げたループの構成部品6つのうち、**「外部メモリ（反復をまたいで記憶を保つ）」**が
これに当たる。我々の実験の弱点そのものだった。

ユーザーが最初に言った「このセッションはいつ消滅するかわからない」という懸念に直接効く。
**Issue はセッションが死んでも残る。**

### 2. 発見と修正で最適な設定が違う

| | 発見ループ | 修正ループ |
|---|---|---|
| 形 | 広く浅く | 狭く深く |
| 並列度 | 高い（多数のエージェントを別観点で） | 低い（1 Issue に集中） |
| 隔離 | 不要（読むだけ） | 必要（worktree） |
| 停止条件 | 「新しい発見がK回連続でゼロ」 | 「test/lint/check が緑 + 検証役OK」 |

同じループに混ぜると両方が中途半端になる。

### 3. 人間が介入する場所が明確になる

**Issue のトリアージが Ng の言う Developer Feedback Loop そのもの。**
「これは直す」「これは仕様」「これは後回し」を人間が決める。
人間の "context advantage" が一番効く場所がここ。

実際、今回も判断が必要だった:
- 404 の文言不一致 → **仕様書側のミス**と判断し、仕様書を実装に合わせた（Issue にしない）
- 認可欠陥 → Issue #2 として切り出し、PATCH と DELETE をセットで直す方針に
- テスト重複 → Issue #3。優先度は低いが記録する価値あり

このうち1つ目は**エージェントには判断できない**。どちらが正しいかは仕様の決定権者しか知らない。

### 4. 両ループが独立に回せる

修正ループが詰まっても発見ループは走り続けられる。

### 補足: Boris Cherny の状態がまさにこれ

> 「私はもうClaudeにプロンプトを打っていない。ループが走っていて、そのループがClaudeに
> プロンプトを打ち、何をすべきか判断している。私の仕事はループを書くことだ」
>
> 2026年に1行もコードを書かず、スマホから1日に何十本ものプルリクエストを承認するだけ

**彼が見ているのは Issue と PR であって、コードではない。**
この構造なしにこの状態には到達できない。

## 実際にやったこと

今日ここまでで見つかった問題7件のうち、未対応だった4件を処理した。

| # | 内容 | 処理 |
|---|---|---|
| 1 | tsconfig が vitest 型を読まない | ✅ PR #1 でマージ済み |
| 2 | `res.json()` が unknown | ✅ PR #1 でマージ済み |
| 3 | CI が vitest を実行していない | ✅ PR #1 でマージ済み |
| 4 | DELETE に認可がない | → **Issue #2**（5と統合） |
| 5 | PATCH に認可がない | → **Issue #2** |
| 6 | 404 の文言が仕様書と不一致 | → **仕様書側を修正**（Issue にしない） |
| 7 | DELETE のテストが重複 | → **Issue #3** |

さらに、ラウンド0の事前調査で見つけていた未処理の問題も Issue 化:

| 内容 | 処理 |
|---|---|
| README が参照する `.example` ファイルが存在しない | → **Issue #4** |

### 作成した Issue

- **#2** [bug] PATCH / DELETE に所有者チェックがない → https://github.com/EngineMaker/world-issue-tracker/issues/2
- **#3** [good first issue] DELETE のテスト1件が重複 → https://github.com/EngineMaker/world-issue-tracker/issues/3
- **#4** [documentation] `.example` ファイルが存在しない → https://github.com/EngineMaker/world-issue-tracker/issues/4

### 出した PR

- **#5** feat: DELETE /issues/:id を追加 → https://github.com/EngineMaker/world-issue-tracker/pull/5

`Closes #2` は**あえて使っていない**。この PR は #2 を解決しないため。
代わりに「この PR の実装にも #2 が該当する」と本文に明記した。

**PR に既知の欠陥があることを本文に書く**のは、Issue キューがあるからできること。
以前なら「直すか、黙ってマージするか」の二択だった。

## Issue の粒度について学んだこと

**同じ根を持つ問題は1つの Issue にまとめる。**

DELETE の認可欠陥（#4）と PATCH の認可欠陥（#5）は別々に発見されたが、
Issue は1つ（#2）にした。理由:

> 片方だけ直しても、攻撃者はもう片方から内容を破壊できるため、実質的な保護にならない。

分けると「#4 だけ直して閉じる」という部分的な対応が起きうる。
**Issue の粒度は「1つ閉じたら実際に問題が解決する」単位にする。**

## 次のステップ

### 未着手: 発見ループの自動化

今回の発見は「人間（と私）が手で見つけたもの」の Issue 化にとどまった。
本来の発見ループは:

- 複数エージェントを**別観点**で並列に走らせる
  （セキュリティ / テスト品質 / 仕様との整合 / 未実装機能 / パフォーマンス）
- 見つけたものを Issue 化
- **重複排除が肝**（同じ問題で Issue が乱立しないように、既存 Issue と照合する）
- 停止条件は「K回連続で新しい発見がゼロ」（loop-until-dry パターン）

### 未着手: フェーズB（停止条件の拡張）

ラウンド1フェーズAで判明した「機械が見逃すもの」を、どこまで機械側に落とせるか。
Issue #2 の修正がそのまま題材になる（認可テストを追加すれば、認可欠陥は機械で捕まえられる）。
