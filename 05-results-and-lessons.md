# 総括: 1日でどこまで行けたか

2026-08-01 〜 08-02。ループエンジニアリングを「聞く → 調べる → 手を動かす → 自動化する →
無人運転する」まで一気に通した記録。

## 成果

題材: [EngineMaker/world-issue-tracker](https://github.com/EngineMaker/world-issue-tracker)
（作りかけの Turborepo + Hono/Workers + Next.js + D1 プロジェクト）

**マージされた PR: 9本 / クローズされた Issue: 7件**

| PR | 内容 | 出どころ |
|---|---|---|
| #1 | 壊れていた typecheck と CI のテスト実行を修正 | 手動 |
| #5 | `DELETE /issues/:id` を追加 | 手動ループ |
| #6 | PATCH / DELETE に所有者チェックを追加 | 自動ループ |
| #7 | DELETE の重複テストを統合 | 自動 + worktree |
| #11 | 公開 GET から Clerk User ID の露出を防ぐ | **発見→修正 連結** |
| #18 | 書き込み系 API に Origin 検証を入れて CSRF を塞ぐ | **無人運転** |
| #19 | 環境変数サンプルと README 検証テスト | 取りこぼし回収 |
| #20 | GET /issues のクエリ検証に異常系テストを追加 | **無人運転** |
| #27 | Clerk 認証テストのモック層を下げる | **無人運転（3度目の正直）** |

**このうち5本は、人間がマージボタンを押しただけ。**
問題の発見、再現の実証、実装、テスト、レビュー、PR 作成はすべて自動。

見つかった実在の脆弱性:
- 誰でも他人の Issue を更新・削除できた（認可の欠落）
- 公開 API が Clerk User ID を未認証の第三者に露出していた
- CSRF 対策が無かった

## 実行統計

| | 発見ループ | 修正ループ |
|---|---|---|
| launchd 起動 | 7回 | 7回 |
| 実際に走った | 4回 | 7回 |
| 早期終了（$0） | **3回** | 0回 |
| 成功 | 4回 | 3回 |
| 失敗（API 接続断） | 0回 | 3回 |

**総コスト: 約 $50**（手動実験を含む1日分）

| 種類 | 時間 | コスト |
|---|---|---|
| 発見ループ1回 | 330〜860s | $1.9〜3.5 |
| 修正ループ1回 | 370〜880s | $2.0〜5.3 |
| **早期終了** | **< 1s** | **$0** |

## 学んだこと（重要な順）

### 1. ループを回す前に、停止条件が機能することを確かめる

**最初の実験で、検証の仕組みが3箇所壊れていた。**

| 壊れていたもの | なぜ気づかれなかったか |
|---|---|
| tsconfig が vitest-pool-workers の型を読まない | vitest は実行時に解決するのでテストは緑 |
| `res.json()` が `unknown` で 40 箇所エラー | 同上。tsc を手で叩かないと見えない |
| CI が vitest ではなく Bun 内蔵ランナーを実行 | **ワークフローが一度も実行されていなかった** |

3つとも「緑に見えていたが、実際には検証できていなかった」という同じ形。

> ループエンジニアリングは機械判定の停止条件に全体重を預ける手法である。
> だからこそ、その停止条件自体が嘘をついていたら、ループは
> **「何も検証していないのに緑を返し続ける装置」**になる。

Ng が「仕様と一緒に evals を渡せ」と言うとき、その evals が正しく動くことは暗黙の前提。
実プロジェクトではその前提が既に崩れていることがある。

### 2. 機械的検証は「壊れていないこと」しか見ていない

test / lint / typecheck が全部緑でも、**「何もしない」が通る**。

実際、3条件すべて緑の実装に**重大な認可欠陥**があった。
検証役が見つけた3件のうち、機械的検証で捕まえられるものは**1つもなかった**。

### 3. 作り手と検証役を分けるのが一番効く

一番はっきり出たのが**ミューテーションテストの応酬**。

**PR #7 の例**:
1. 作り手が「重複テストを削除しても検出力は落ちない」と主張。
   `requireAuth` を外す変異体で検証
2. **レビュアーが反証** — 「401 を返しつつ削除は実行する」という別の変異体を構築。
   これは削除しようとしているテストだけが捕まえていた
3. 作り手が再現して認め、削除ではなく統合する形に修正

**PR #11 の例**:
- レビュアー: 「公開フィールドのテストがクエリパラメータなしでしか叩いていないため、
  **フィルタ経路でだけ漏れる回帰は検出できない**」
- 作り手が変異体 M11（`?scope=` 指定時だけ漏らす）で再現し、テストを追加

**作り手の自己検証を、レビュアーがより強い変異体で破る**という構図が繰り返し起きた。
この知見はプロンプトに反映した:

> ステータスコードを変える変異体だけでなく、**副作用を変える変異体も試す**よう
> 指示してください。前者しか試さないと、テストの検出力を過大評価します。

### 4. Issue をキューにすると構造が変わる

ユーザーの提案:
> バグを発見するループと修正するループをわけて、GitHub の issue に記録して、
> ループの主目的は issue をこなすことにしたら

これでループが連結した:

```
発見ループ → Issue → [人間がトリアージ] → 修正ループ → PR → [人間がマージ] → Issue 自動クローズ
```

利点:
- **Issue が外部メモリになる**（セッションが死んでも残る）— Osmani の6部品のひとつ
- 発見（広く浅く並列）と修正（狭く深く隔離）で最適な設定が違うので、分けると両方が本来の形になる
- **人間が介入する場所が明確**になる。トリアージが Ng の言う Developer Feedback Loop そのもの
- **PR に既知の欠陥があることを本文に書ける**ようになった。
  以前は「直すか黙ってマージするか」の二択だった

### 5. 無人ループの最大のリスクは「失敗が静かに永続化する」こと

一時的な API 接続断で修正ループが3回死んだ。**それ自体は避けられない。**

まずかったのは後始末が無かったこと:
- 失敗した実行が空ブランチを残す
- 着手済み判定が「ブランチがあればスキップ」
- → **その Issue は以後永久に処理されない**
- ログには WARN が出るが、無人運転では誰も見ていない

設計すべきなのは:
1. **失敗を検知できるか** — WARN をログに出すだけでは足りない
2. **失敗が次回に持ち越されないか** — 残骸が「着手済み」と誤判定されないか
3. **成果が失われないか** — 完成した仕事が push 漏れで埋もれないか

今回は3つとも不備があった。**放置して初めて分かる種類の欠陥。**

修正後、実地で機能した:
```
[00:49] WARN: claude が非ゼロ終了
[01:00] Issue #10 の空ブランチ fix/issue-10 を回収する（失敗した実行の残骸）
[03:40] PR ができた: .../pull/27      ← 3度目の正直
```

### 6. 禁止したら、代わりに何をすべきかを必ず書く

ユーザーの指摘:
> `bun install` は禁止ですって書いてたけど、なぜ禁止なのか理由がわからないのと、
> もし妥当な理由があって禁止されてたら人間に依頼して欲しいのですよ

プロンプトには禁止だけ書いてあり、新しい依存が必要になった場合の導線がなかった。
エージェントは詰むか、最悪リンクを壊してでも進める。

> **人間なら聞きに来るが、ループは聞きに来る先がないと詰むか暴走する。**

### 7. 長時間走ると冒頭の指示は薄れる

76ターンの作業中、発話が英語に戻っていた（言語指定は冒頭にあった）。
特に**デバッグで難航した局面**で顕著。

対策: 冒頭の指定を強め、**末尾にも再掲**を置いた。次の実行で日本語が維持されることを確認。

### 8. 進捗の可視化は運用可能性の問題

`claude -p` のデフォルト（text 形式）は「全処理が終わってから一括出力」。
数分〜十数分かかる処理ではログが空のままで、「動いていない」と誤認する。

`--output-format stream-json --verbose` + jq で整形して解決:

```
💬 まず現状を確認します
🔧 Bash: git status && git log --oneline -3
🤖 Task(general-purpose): レビュアーとして検証
── 終了 (success) turns=28 時間=370s コスト=$2.017
```

> 無人ループで一番困るのは「動いているのか死んでいるのか分からない」状態。

### 9. ノートPCでの無人運転はスリープが前提条件

深夜の実行で2件の異常が出た:
- 23:38 の実行が起動直後に消えた（ログファイル作成前）
- 01:39 の実行が claude 856秒に対し実時間1時間半

原因は**マシンのスリープ**（バッテリー駆動、`Maintenance Sleep` を繰り返していた）。
ループの不具合ではない。

electron や cron を含め、ローカルマシンでの定期実行には
**電源接続とスリープ設定**が実質的な前提になる。

### 10. マージが人間のボトルネックになる

発見ループが Issue を作る速度に、修正ループ＋人間のマージが追いつかない。

1日で **Issue 12件がオープンのまま**残った。バックログ上限（15件）に近づいたため、
**発見ループを停止して修正ループにバックログを消化させる**判断をした。

これは Issue-driven ループの構造的な性質。
Cherny の「1日に何十本ものPRを承認する」は、**承認の速度が律速になる**ことを示している。

## ハマった実装バグ

自動化で最初にぶつかったのは、ループの設計ではなく**シェルスクリプトの初歩的な問題**だった。

| # | バグ | 症状 |
|---|---|---|
| 1 | bash 3.2 の空配列展開（`set -u`） | `unbound variable` で即死 |
| 2 | **stdin を明示していない** | claude が入力待ちで固まり、ログが0バイト |
| 3 | text 形式は処理完了まで無出力 | 「動いていない」と誤認 |
| 4 | **日本語の直後の変数展開** | `$LENS_NAME」` が変数名と解釈され `unbound variable` |
| 5 | launchd の PATH が最小限 | `claude` も `gh` も見つからない |
| 6 | 認証が Keychain にある | `HOME` と `USER` が必要 |

2 と 4 は特に厄介で、どちらも「初回実行で必ず踏むが dry-run では通る」種類だった。

## 未検証のまま残っていること

- **バックログ上限の実地検証** — Issue が15件を超えたときに発見ループが止まるか
- **発見ループの周回数を増やしたときの重複** — 2周目では重複しなかったが、
  5周・10周と回したときは未検証
- **並行実行の限界** — 修正ループを2本同時に走らせてはいない（worktree で隔離済みだが未検証）
- **モデルへの信頼度に応じた自動マージ** — 今は人間が必ずマージする
